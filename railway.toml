# Railway Configuration for GameFlowData
# Single worker service that runs scheduled jobs via APScheduler

[build]
builder = "nixpacks"

[deploy]
startCommand = "python src/orchestration/scheduler.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
